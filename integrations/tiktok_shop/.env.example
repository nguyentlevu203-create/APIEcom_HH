# App Key is not secret (it's visible in TikTok Partner Center), so it can
# live here to save re-typing it at every script run.
TIKTOK_APP_KEY=6jh02dvvivnis

# Do NOT put App Secret, auth_code, access_token, or refresh_token in this
# file, in any .env file, or anywhere in Git. The scripts always prompt for
# App Secret via hidden input instead.
